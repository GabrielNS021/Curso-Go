# Aprendendo-Go

## Anotações
fmt.Print("Linha Continua\n")
fmt.Println("Quebra linha")
fmt.Printf("Formata %s!", "linha")

const/var (
    a = 1
    b = 2
)

var g, h, i := 2, false, "epa!"

import m "math"
